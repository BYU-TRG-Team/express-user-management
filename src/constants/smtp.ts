import { APP_ROOT } from "@constants";

export const DEFAULT_PASSWORD_RESET_EMAIL_TEMPLATE = `${APP_ROOT}/email-templates/password-reset`;
export const DEFAULT_VERIFICATION_EMAIL_TEMPALTE = `${APP_ROOT}/email-templates/verification`;